using FootMatcher.Models.Models;

namespace FootMatcher.InitialDataFileParser
{
    public class FileSystemDbInitialDataFileParser
    {
        public List<Team> Parse(IEnumerable<string> lines)
        {
            return new List<Team>();
        }
    }
}